# Python
__pycache__/
*.pyc

# Local runtime
*.log

# Keep your API key out of version control.
# config.json is tracked, but never commit a real key in it — use the
# ANTHROPIC_API_KEY environment variable instead, or uncomment the next line:
# config.json
